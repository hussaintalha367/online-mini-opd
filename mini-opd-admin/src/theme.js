import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#2E86C1",
    },
    secondary: {
      main: "#58D68D",
    },
    background: {
      default: "#F4F6F7",
    },
    error: {
      main: "#E74C3C",
    },
  },
});

export default theme;